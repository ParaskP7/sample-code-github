<?xml version="1.0" encoding="utf-8"?>

<resources>

    <!-- SEARCH -->

    <style name="SearchToolbarIcon">
        <item name="android:layout_gravity">center</item>
        <item name="android:paddingEnd">20dp</item>
        <item name="android:paddingStart">20dp</item>
        <item name="android:src">@drawable/ic_search</item>
    </style>

    <style name="SearchToolbarEditText">
        <item name="android:gravity">center_vertical</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorHint">@android:color/darker_gray</item>
        <item name="android:inputType">text</item>
        <item name="android:maxLength">30</item>
        <item name="android:maxLines">1</item>
        <item name="android:lines">1</item>
        <item name="android:singleLine">true</item>
        <item name="android:imeOptions">actionSearch</item>
    </style>

    <!-- REPOSITORY -->

    <style name="RepositoryToolbarIcon">
        <item name="android:layout_gravity">center</item>
        <item name="android:paddingEnd">20dp</item>
        <item name="android:paddingStart">20dp</item>
        <item name="android:src">@drawable/ic_arrow_back</item>
    </style>

    <style name="RepositoryToolbarTextView">
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">18sp</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
    </style>

    <style name="RepositoryToolbarAvatar">
        <item name="android:layout_gravity">center</item>
        <item name="android:paddingEnd">20dp</item>
        <item name="android:paddingStart">20dp</item>
        <item name="android:scaleType">center</item>
    </style>

</resources>
